#include "epoll_wrapper.h"

namespace rrr {

// Note: Wait_One() and Wait_Two() methods removed - not in header
// Wait() is inline in the header, so no implementation needed here

} // namespace rrr
